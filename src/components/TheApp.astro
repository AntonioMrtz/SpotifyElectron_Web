---
import home from "../../public/walktrough/Home.png?url";
import playlist from "../../public/walktrough/Playlist.png?url";
import uploadSong from "../../public/walktrough/UploadSong.png?url";
import explorar from "../../public/walktrough/Explorar.png?url";
---

<div class="theapp-section section-left">
    <img class="img-fluid" src={home} alt="" />

    <div class="theapp-section-header">
        <h3 class="reveal">Explore the best artists and music</h3>
        <p>
            All of your favourites artists and songs are now in SpotifyElectron. Lets deep in and find all the new features.
        </p>
    </div>
</div>
<div class="theapp-section section-right">
    <div class="theapp-section-header">
        <h3 class="reveal">Organize your favorite music in playlists</h3>
        <p>
            Save your songs into playlist and access them from wherever and
            whenever you want. Access your friends playlist for sharing and discovering new
            music.
        </p>
    </div>

    <img class="img-fluid" src={playlist} alt="" />
</div>

<div class="theapp-section section-left">
    <img class="img-fluid" src={uploadSong} alt="" />

    <div class="theapp-section-header">
        <h3 class="reveal">Upload your songs</h3>
        <p>Have you missed not finding a not so popular song in other streaming services? Thats not the 
            problem from now on, SpotifyElectron allows users to upload their own music and share them globally.
        </p>
    </div>
</div>

<div class="theapp-section section-right">
    <div class="theapp-section-header">
        <h3 class="reveal">Discover and try the new interface</h3>
        <p>Modern interface with the best user experience. Search by genre, find your favourite songs or see 
            what your friends are playing : the limit its up to you. 

        </p>
    </div>

    <img class="img-fluid" src={explorar} alt="" />
</div>

<style>
    .theapp-section {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        margin-bottom: 8rem;
        padding: 0 1rem 0 1rem;
    }

    .theapp-section img {
        width: 40svw;
        border-radius: 8px;
    }
    .theapp-section-header {
        display: flex;
        flex-direction: column;
    }

    .theapp-section-header h3 {
        padding-bottom: 1%;
        border-bottom: 2px solid var(--app-logo-color-darker);
    }

    .theapp-section-header h3::first-letter {
        color: var(--app-logo-color-darker);
    }

    .theapp-section-header p {
        color: var(--subheader-text);
    }

    .section-left img {
        margin-right: 3rem;
        filter: drop-shadow(-20px 15px 10px var(--secondary-color));
    }

    .section-right img {
        margin-left: 3rem;
        filter: drop-shadow(20px 15px 10px var(--secondary-color));
    }

    @media (max-width: 989px) {
        .theapp-section {
            flex-direction: column;
            padding: 0 2.5rem 0 2.5rem;
        }
        .theapp-section img {
            margin: 2rem 0 0 0 !important;
            width: 80svw;
            filter: drop-shadow(0px 0px 10px var(--secondary-color));
        }

        .section-left .theapp-section-header {
            order: 1;
        }
        .section-left img {
            order: 2;
        }
    }

    /* Animation */

    .swing-in-left-fwd {
        -webkit-animation: swing-in-left-fwd 0.5s
            cubic-bezier(0.175, 0.885, 0.32, 1.275) both;
        animation: swing-in-left-fwd 0.5s
            cubic-bezier(0.175, 0.885, 0.32, 1.275) both;
    }

    @-webkit-keyframes swing-in-left-fwd {
        0% {
            -webkit-transform: rotateY(100deg);
            transform: rotateY(100deg);
            -webkit-transform-origin: left;
            transform-origin: left;
            opacity: 0;
        }
        100% {
            -webkit-transform: rotateY(0);
            transform: rotateY(0);
            -webkit-transform-origin: left;
            transform-origin: left;
            opacity: 1;
        }
    }
    @keyframes swing-in-left-fwd {
        0% {
            -webkit-transform: rotateY(100deg);
            transform: rotateY(100deg);
            -webkit-transform-origin: left;
            transform-origin: left;
            opacity: 0;
        }
        100% {
            -webkit-transform: rotateY(0);
            transform: rotateY(0);
            -webkit-transform-origin: left;
            transform-origin: left;
            opacity: 1;
        }
    }
</style>

<script>
    function reveal() {
        var reveals = document.querySelectorAll(".reveal");

        for (var i = 0; i < reveals.length; i++) {
            var windowHeight = window.innerHeight;
            var elementTop = reveals[i].getBoundingClientRect().top;
            var elementVisible = 150;

            if (elementTop < windowHeight - elementVisible) {
                reveals[i].classList.add("swing-in-left-fwd");
            } else {
                reveals[i].classList.remove("swing-in-left-fwd");
            }
        }
    }

    window.addEventListener("scroll", reveal);
</script>
